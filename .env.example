# Copy this to .env (or export vars) and fill in your values
OPENAI_API_KEY=sk-...
# A vision-capable model name, e.g. gpt-4o-mini (or your preferred)
OPENAI_MODEL=gpt-4o-mini

# Optional overrides (leave empty if not needed)
OPENAI_BASE_URL=
OPENAI_API_VERSION=
